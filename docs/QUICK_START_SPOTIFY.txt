╔════════════════════════════════════════════════════════════════╗
║          🎵 SPOTIFY LOCAL SETUP - QUICK START GUIDE           ║
╚════════════════════════════════════════════════════════════════╝

📋 WHAT YOU NEED:
✅ Python 3.11 or newer
✅ Spotify account (free or premium)
✅ 10 minutes of your time

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

⚡ SUPER QUICK SETUP (5 Steps):

1️⃣  DOWNLOAD PROJECT
   Download this project to your computer

2️⃣  INSTALL PACKAGES
   Open Terminal/Command Prompt and run:
   
   pip install requests python-dotenv

3️⃣  GET SPOTIFY CREDENTIALS
   a) Go to: https://developer.spotify.com/dashboard
   b) Click "Create App"
   c) App name: "My Music Controller"
   d) Redirect URI: http://localhost:8888/callback
   e) Copy your Client ID and Client Secret

4️⃣  CREATE .env FILE
   Create a file named ".env" in your project folder:
   
   SPOTIFY_CLIENT_ID=paste_your_client_id_here
   SPOTIFY_CLIENT_SECRET=paste_your_client_secret_here
   SPOTIFY_REDIRECT_URI=http://localhost:8888/callback

5️⃣  RUN & AUTHENTICATE
   python spotify_local.py
   
   Follow the prompts to authenticate (one time only)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🎮 USING SPOTIFY IN YOUR CODE:

from dotenv import load_dotenv
from spotify_local import SpotifyLocal

load_dotenv()
spotify = SpotifyLocal()
spotify.authenticate()  # One time only

# Now control Spotify!
print(spotify.get_current_track())
print(spotify.play())
print(spotify.pause())
print(spotify.next_track())
print(spotify.previous_track())
print(spotify.set_volume(50))
print(spotify.search('Bohemian Rhapsody'))
print(spotify.get_playlists())

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🎯 AVAILABLE COMMANDS:

▶️  spotify.play()              - Resume playback
⏸️  spotify.pause()             - Pause music
⏭️  spotify.next_track()        - Next song
⏮️  spotify.previous_track()    - Previous song
🔊  spotify.set_volume(50)      - Set volume to 50%
🎵  spotify.get_current_track() - What's playing?
🔍  spotify.search('song name') - Search Spotify
📚  spotify.get_playlists()     - List your playlists

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

⚠️  IMPORTANT:
- Spotify must be running on at least one device (desktop app or web player)
- Make sure you start playing music before using the API
- Authentication token expires - just run authenticate() again if needed

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📖 DETAILED GUIDE:
Read SPOTIFY_LOCAL_SETUP.md for complete step-by-step instructions

🐛 TROUBLESHOOTING:
- "No active device" → Open Spotify and start playing
- "Invalid credentials" → Check your .env file
- "Module not found" → Run: pip install requests python-dotenv

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ THAT'S IT! You're ready to control Spotify from Python! 🎵
